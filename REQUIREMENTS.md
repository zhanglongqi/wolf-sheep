# 狼吃羊 — 需求文档 v2

> 传统中国民间棋盘游戏数字复现 · 两种棋盘 · 三种对局模式 · Phaser 4 + Vite

---

## 01 概述

双方对弈：**狼方**通过跳吃消灭羊；**羊方**通过数量包围狼直至其无路可走。

| 维度     | 棋盘一：方格棋盘      | 棋盘二：传统棋盘                    |
|----------|----------------------|------------------------------------|
| 棋盘拓扑 | 5×5 方格，横纵连线    | 3×3 主棋盘 + 对角线 + 上下菱形延伸 |
| 总节点数 | 25                   | 19（主棋盘 9 + 上菱形 5 + 下菱形 5） |
| 狼数量   | 3                    | 2                                  |
| 狼起始位 | 第 0 行的第 0、2、4 列 | 上、下两个"脖子"节点                |
| 羊数量   | 15（全部初始入场）     | ≈30（初始 8 只在中圈，储备逐步入场）  |
| 移动方向 | 四方向（上/下/左/右）  | 沿所有棋盘连线（含对角线）           |

---

## 02 棋盘布局

### 棋盘一：方格棋盘

```
列:  0    1    2    3    4
行0: 狼   ·    狼   ·    狼    ← 狼起始行
行1: ·    ·    ·    ·    ·
行2: 羊   羊   羊   羊   羊
行3: 羊   羊   羊   羊   羊
行4: 羊   羊   羊   羊   羊
```

### 棋盘二：传统棋盘（节点拓扑）

```
         apex_t            ← 顶点
        /   |   \
     left_t  ctr_t  right_t  ← 菱形（X 形连线）
        \   |   /
         neck_t            ← 上脖子（狼1起始位）
        / \ | / \
      TL   TC   TR          ← 主棋盘顶行（羊中圈）
      |\ X  |  X /|
      ML   MC   MR          ← 主棋盘中行（MC 初始为空）
      |/ X  |  X \|
      BL   BC   BR          ← 主棋盘底行（羊中圈）
        \ \ | / /
         neck_b            ← 下脖子（狼2起始位）
        /   |   \
    left_b ctr_b right_b   ← 菱形（X 形连线）
        \   |   /
         apex_b            ← 底点
```

**节点说明：**

- **脖子（neck_t / neck_b）**：菱形与主棋盘的连接点，狼的起始位置
- **羊中圈（8节点）**：TL、TC、TR、ML、MR、BL、BC、BR（主棋盘外圈）
- **主棋盘中心（MC）**：初始为空，是对角线交叉的枢纽节点

**主棋盘连线：**

- 横线：TL–TC–TR，ML–MC–MR，BL–BC–BR
- 纵线：TL–ML–BL，TC–MC–BC，TR–MR–BR
- 对角线（每格内的 X）：TL↔MC，TC↔ML，TC↔MR，TR↔MC，ML↔BC，MC↔BL，MC↔BR，MR↔BC

---

## 03 走棋规则

### 狼方

| 操作               | 规则                                                                               |
|--------------------|------------------------------------------------------------------------------------|
| **移步**           | 沿棋盘连线，走到紧邻的空位（一步）                                                    |
| **跳吃（隔一步吃）** | 狼在 A，紧邻 B 有羊，B 背后的 C 是空位，且 A–B–C 在同一连线上 → 狼跳至 C，B 处的羊消除 |

> 每次只能跳过一只羊；不支持连续多跳。

### 羊方

| 阶段                       | 条件                    | 操作                                                    |
|----------------------------|-------------------------|---------------------------------------------------------|
| **放置阶段**（传统棋盘专属） | 储备羊数 > 0            | 每回合将一只新羊放置到任意空位；已放置的羊此阶段不可移动 |
| **移动阶段**               | 储备羊数 = 0，或方格棋盘 | 将任意一只存活的羊沿棋盘连线走到紧邻空位（一步）          |

### 堵死机制

若狼某方向紧邻羊，且该羊背后无空位（出界或被另一棋子占据），则该方向对狼**完全封死**——既不能移步，也不能跳吃。

---

## 04 胜负条件

| 结果         | 判定条件                                                                                                                  |
|--------------|---------------------------------------------------------------------------------------------------------------------------|
| **狼方获胜** | 棋盘上所有羊被吃光（存活羊数量 = 0）                                                                                        |
| **羊方获胜** | 棋盘上**所有狼同时**无任何合法操作——每只狼的每个方向均无可走空位，且无"羊+背后空位"组合。只要有任意一只狼仍可行动，羊方未胜。 |

---

## 05 回合流程

```
游戏开始（狼方先行）
  │
  ▼
┌─────────────────────────────────┐
│ 当前方行动                       │
│  1. 点击己方棋子（高亮合法目标）  │
│     · 蓝色圆点 = 可移步目标      │
│     · 橙色圆点 = 可跳吃落点      │
│  2. 点击目标格确认执行            │
│     · 跳吃：被跨越的羊立即消失   │
│     · 再次点击棋子可取消选中      │
└──────────────┬──────────────────┘
               │
               ▼
        检查胜负条件
        ├─ 羊归零 → 狼方获胜，弹出终局弹窗
        ├─ 所有狼无合法操作 → 羊方获胜，弹出终局弹窗
        └─ 未触发 → 换手，进入对方回合
```

**传统棋盘阶段切换：** 羊方回合开始时，若储备 > 0 执行「放置」，储备 = 0 自动切换为「移动」，无需额外确认。

---

## 06 UI 需求

### 顶部信息栏（常驻）

| 元素     | 内容                   | 备注                    |
|----------|------------------------|-------------------------|
| 回合提示 | 「请狼走」/ 「请羊走」     | 与当前轮次同步          |
| 存活羊数 | 当前棋盘上的羊数量     | 实时更新                |
| 储备羊数 | 「未下的羊：N」           | 仅传统棋盘，放置阶段显示 |
| 阶段标识 | 「放置阶段」/ 「移动阶段」 | 仅传统棋盘显示          |

### 棋子交互反馈

| 交互             | 视觉反馈                             |
|------------------|--------------------------------------|
| 选中棋子         | 白色描边高亮环                       |
| 可移步目标格     | 蓝色半透明圆点                       |
| 可跳吃目标格     | 橙色半透明圆点；被吃羊标红轮廓        |
| 无合法操作的棋子 | 降低不透明度至 40%，不可点选          |
| 执行移步         | 棋子平移动画（150 ms）                 |
| 执行跳吃         | 棋子平移（150 ms）+ 被吃羊淡出（200 ms） |

### 游戏模式

| 模式         | 狼方                      | 羊方               |
|--------------|---------------------------|--------------------|
| A · 操控狼   | 玩家                      | AI（随机合法操作）   |
| B · 操控羊   | AI（优先跳吃，否则随机移步） | 玩家               |
| C · 双人对战 | 玩家 1                    | 玩家 2（同设备轮流） |

### 控制按钮与弹窗

| 控件     | 功能                                                |
|----------|-----------------------------------------------------|
| 设置     | 选择棋盘类型（棋盘一 / 棋盘二）及游戏模式（A / B / C）  |
| 重新开始 | 恢复初始棋盘状态，重置所有棋子和计数                 |
| 终局弹窗 | 覆盖层显示「狼方获胜！」或「羊方获胜！」，附「再来一局」按钮 |

---

## 07 技术架构

### 对 `src/game.js` 的改造方向

```
src/game.js
├── const BOARD_CONFIGS          ← 两种棋盘的节点坐标 + adjacency map
│   ├── grid5x5                  ← 棋盘一：25节点，四方向邻接
│   └── traditional              ← 棋盘二：19节点，含对角连线
│
├── class Piece                  ← 原 Cheese；保留 x/y/type/alive/graphics
│
├── class Board                  ← 原 WolfSheep；管理棋盘状态 + 规则
│   ├── getValidMoves(piece)     ← 返回可走的空位节点列表
│   ├── getValidEats(piece)      ← 返回 [{landAt, remove}] 跳吃选项
│   ├── move(piece, nodeId)      ← 执行移步
│   ├── eat(piece, landId)       ← 执行跳吃，消除中间的羊
│   ├── placeSheep(nodeId)       ← 传统棋盘：将储备羊放置到节点（新增）
│   └── checkWin()               ← 返回 'wolf' | 'sheep' | null
│
├── class AIPlayer               ← 新增：AI 操作选择逻辑
│   ├── makeWolfMove(board)      ← 优先选择跳吃，否则随机移步
│   └── makeSheepMove(board)     ← 随机放置或移动
│
└── class GameScene              ← 原 Example（Phaser.Scene）
    ├── create()                 ← 初始化棋盘、UI 按钮、指针事件
    ├── onPointerDown(x, y)      ← 两段式：选棋子 → 确认目标
    ├── highlightMoves(piece)    ← 绘制蓝/橙高亮标记
    ├── clearHighlights()
    ├── nextTurn()               ← 换手 + 触发 AI 回合
    └── showResult(winner)       ← 终局弹窗
```

### 关键数据结构：adjacency map

节点用字符串 ID 标识，adjacency map 存储每个节点的直连邻居列表，是跳吃与移步合法性判断的核心依据。

```js
// 传统棋盘节点示例
const traditional = {
  nodes: {
    neck_t: { x: 90, y: 92 }, // 上脖子（狼1起始位）
    TL: { x: 50, y: 127 }, // 主棋盘左上（羊中圈）
    TC: { x: 90, y: 127 }, // 主棋盘中上（羊中圈）
    MC: { x: 90, y: 167 }, // 主棋盘正中（初始为空）
    // ... 共 19 个节点
  },
  adjacency: {
    neck_t: ["top_left", "top_center", "top_right", "TL", "TC", "TR"],
    TL: ["neck_t", "TC", "ML", "MC"],
    MC: ["TL", "TC", "TR", "ML", "MR", "BL", "BC", "BR"],
    // ...
  },
};
```

---

## 08 验收清单

- [ ] 方格棋盘：3 狼 15 羊全部上场，点击可正常走棋
- [ ] 传统棋盘：2 狼在脖子，8 羊在中圈，储备羊逐步入场
- [ ] 狼跳吃正确：落点 = 被吃羊对面空位，羊立即消失
- [ ] 狼无法直接踩入有棋子的节点（移步合法性校验）
- [ ] 堵死检测：紧邻羊且背后无空位的方向对狼封死
- [ ] 所有狼同时无合法操作 → 弹出「羊方获胜」
- [ ] 所有羊被吃完 → 弹出「狼方获胜」
- [ ] 传统棋盘储备归零后自动切换至移动阶段
- [ ] 三种游戏模式（A/B/C）均可运行，AI 回合自动执行
- [ ] 「重新开始」完整恢复初始棋盘及所有计数
